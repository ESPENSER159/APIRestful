name: Build Frontend

on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: docker build backend
      run:
        docker build ./server -t backend